{
  "scripts": {
    "server:start": "docker-compose up -d",
    "server:stop": "docker-compose down",
    "client": "npx ts-node ./main.ts",
    "start": "yarn run server:start && sleep 2 && yarn run client",
    "stop": "yarn run server:stop"
  },
  "dependencies": {
    "readline": "^1.3.0",
    "socket.io-client": "^4.6.1",
    "typescript": ">=4.4"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "@typescript-eslint/eslint-plugin": "^5.55.0",
    "@typescript-eslint/parser": "^5.55.0",
    "eslint": ">=8.0.0",
    "prettier": "2.8.4",
    "tsconfig.json": "^1.0.11"
  }
}
